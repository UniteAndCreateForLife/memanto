{
  "hooks": {
    "sessionStart": [
      {
        "command": "\"${SYS_EXECUTABLE}\" \"${HOOKS_DIR}/session_start.py\" --host cursor"
      }
    ],
    "preCompact": [
      {
        "command": "\"${SYS_EXECUTABLE}\" \"${HOOKS_DIR}/session_start.py\" --host cursor"
      }
    ]
  }
}
